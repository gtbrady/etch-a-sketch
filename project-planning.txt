Requirements
- 16x16 grid of square divs (initially)
--- create the divs with JS
--- nest them inside a container div (this can be created in HTML)
--- Use Flexbox (not Grid)
- Hover effect
--- grid divs change color when the mouse passes over them
- Add button to control grid size
--- Prompts user for # of squares per side
--- Limit input to 100 or less
--- Once entered, existing grid is replaced. It should take up same total pixels as original grid

Extra Credit
1. Randomize the square color with each interaction
2. Progressive darkening effect, where each interaction darkens the square by 10%

~~~~~~~~~~~~~~~~~~~~~~~~
Planning
need reference to grid container that is part of the HTML
createGrid(integer) - creates a grid of integer * integer number of divs
then need to add flexbox styling to grid so it lays out correctly. 
Need to hard code the grid size in pixels. 
Setup the hover effect so the grid changes colors when you hover the mouse
add button to top of screen, event handler prompts user for an integer from 1 - 100
creates a new square grid based off the user input
then tackle extra credit